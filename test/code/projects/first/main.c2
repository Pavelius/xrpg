import geo.rect;
import core.crt;import core.string as str;
import core.io;
import sys.platform.ui;
enum {
	OK, Cancel, Accept
};

static int x;
static int y;

void print(int x, int y, char* format) {
    str.print(format, 1, 10);
}

int main() {
	char* test = "Test another";	char temp[260];
	rect rc;}